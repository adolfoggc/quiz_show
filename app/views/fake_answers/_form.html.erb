<%= form_with(model: fake_answer, local: true) do |form| %>
  <% if fake_answer.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(fake_answer.errors.count, "error") %> prohibited this fake_answer from being saved:</h2>

      <ul>
      <% fake_answer.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :answer %>
    <%= form.text_field :answer %>
  </div>
  
  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
